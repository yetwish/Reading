<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.NestedScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:orientation="vertical">
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    <EditText
        android:visibility="gone"
        android:id="@+id/etNoteTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:hint="@string/note_plz_enter_title"
        android:padding="@dimen/menu_margin_left"
        android:textColor="@color/colorSlideMenu"
        android:textColorHint="@color/colorLine"
        android:textSize="@dimen/note_title_text_size" />


    <EditText
        android:id="@+id/etNoteContent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@null"
        android:gravity="left"
        android:hint="@string/note_plz_enter_content"
        android:padding="@dimen/menu_margin_left"
        android:textColor="@color/colorText"
        android:textColorHint="@color/colorLine"
        android:textSize="@dimen/note_content_text_size" />
    </LinearLayout>
</android.support.v4.widget.NestedScrollView>